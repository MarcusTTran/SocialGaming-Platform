#include "GameParser.h"
#include "game.h"
#include <iostream>
#include <string>

int main() {
    std::string filename = "../config/minimal.game";
    // TODO: need to pass a connection in order to call parser.
    return 0;
}